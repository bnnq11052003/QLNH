@model IEnumerable<QLNHxMVC.Models.TbDmfood>

@{
    var doanhmuc = ViewBag.dm;
    ViewData["Title"] = "Doanh Mục";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@* <style>
    /* CSS styles */
    .dialog {
        display: none;
        position: fixed;
        z-index: 1;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        overflow: auto;
        background-color: rgba(0,0,0,0.4);
    }

    .dialog-content {
        background-color: #fefefe;
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        padding: 20px;
        border: 1px solid #888;
    }

    .close {
        color: #aaa;
        float: right;
        font-size: 28px;
        font-weight: bold;
        cursor: pointer;
    }

        .close:hover {
            color: black;
            text-decoration: none;
        }
</style> *@

<h1>Doanh Mục Thức Ăn</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Thêm Doanh Mục</a>
</p>

<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead class="thead-dark">
            <tr>
                <th>
                    Số Thứ Tự
                </th>
                <th>
                    Tên Danh Mục
                </th>
                <th>
                   
                </th>
            </tr>
        </thead>
        <tbody>
            @if(true){
            int i = 1;
            @foreach (var item in doanhmuc)
            {
                
                <tr>
                    <td>
                        @i
                    </td>
                    <td>
                        @item.CategoryName
                    </td>
                    <td>
                        <div class="btn-group" role="group">
                            @* <a asp-action="Edit" asp-route-id="@item.DmfoodId" class="btn btn-sm btn-primary">Edit</a> *@
                            @* <a asp-action="Details" asp-route-id="@item.DmfoodId" class="btn btn-sm btn-info">Details</a> *@
                            
                            <a   asp-action="Delete" asp-route-id="@item.DmfoodId" class="btn btn-sm btn-danger ">Delete</a>
                        </div>
                    </td>
                </tr>
                i++;
            }
            }
        </tbody>
    </table>
</div>

